apply plugin: 'java'

sourceCompatibility = 1.5
targetCompatibility = 1.5
version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'Webmachine4J', 'Implementation-Version': version
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compile(
            'javax.servlet:servlet-api:2.5'
    )
    testCompile group: 'junit', name: 'junit', version: '4.+'
}